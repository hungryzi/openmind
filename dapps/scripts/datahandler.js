
// read file: test.json
var data = JSON.Parse(data)
var l = data.length;
var contributions = {};
for(var i = 0; i < l; i++) {
  data[0]._id = // username
  // check exist
  // if not exist:
  contributions["_id"] = 1;
  // if exist:
  contributions["_id"] += 1;
}
